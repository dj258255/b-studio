# b-studio에 기여하기

b-studio는 모델이 생성한 코드를 실제 서비스처럼 실행하고 검증하는 도구이므로, 기능 동작뿐 아니라 격리·복구·실패 경로를 함께 확인합니다.

## 개발 환경

- Node.js 22 이상
- pnpm 10.29.3
- Docker Desktop 또는 Colima

```bash
corepack enable
pnpm install
pnpm test
pnpm typecheck
```

웹 스튜디오를 개발할 때는 `pnpm studio:demo`, lint, production build도 확인합니다.

## 작업 흐름

1. **착수 명세 이슈**: 새 기능·개선은 "착수 명세" 양식으로 이슈를 엽니다. 문제·선택지·완료 조건·산출물·예상 시간(범위)·주요 불확실성이 필수입니다. 여러 이슈를 묶는 일은 `tracking` 라벨 이슈로 추적합니다.
2. **마일스톤**: 이슈를 마일스톤에 넣습니다. 마일스톤 기한은 이슈 예상의 합에 불확실성을 더해 정합니다.
3. **브랜치**: `main`에서 `feature/<짧은-이름>`, `fix/...`, `docs/...`로 땁니다. `main`에 직접 커밋하지 않습니다.
4. **커밋**: [Conventional Commits](https://www.conventionalcommits.org/) 형식 `type(scope): 설명`을 씁니다. 예: `feat(cli): verify working-tree changes through the same gate as the agent`. 본문에 `Refs #이슈`를 적습니다.
5. **PR**: [PR 템플릿](.github/pull_request_template.md)의 모든 절을 채웁니다. 특히 "돌리지 않은 검증과 이유"와 "예상과 실제"를 채웁니다. PR 하나는 개념 하나를 담습니다.
6. **병합**: squash 병합합니다. PR 제목이 `main`의 커밋 제목이 되므로 무엇이 바뀌는지 알 수 있게 씁니다.
7. **병합 뒤**: [CHANGELOG.md](CHANGELOG.md)의 "아직 릴리스하지 않음"에 한 줄을 더하고, [ROADMAP.md](ROADMAP.md)의 상태와 실제 시간을 갱신합니다.

## 예상을 적고 갱신하기

- 예상은 한 값이 아니라 범위로 씁니다(`2~3시간`). 범위를 가르는 불확실성을 같이 적습니다.
- 진행 중 예상이 틀어질 것이 보이면(범위 상한을 넘을 것으로 보이거나 완료 조건을 바꿔야 할 때) **그때 바로** 이슈에 갱신 댓글을 남깁니다. 댓글에는 끝난 것 / 남은 것 / 새 예상과 그 이유를 적습니다.

  ```
  구현은 끝났고 실제 Docker E2E에서 통합 세션이 디스크 부족으로 실패했다. 원인 확인과 재실행에 1~2시간 더 필요해 완료 예상은 내일 오전으로 바꾼다.
  ```

- 틀린 예상을 지우지 않습니다. PR의 "예상과 실제" 표에 차이 원인을 적습니다.
- 2026-09-24 이전 작업은 예상을 남기지 않았습니다(README "일정과 작업 방식" 참조).

## 실험

- 결정 전에 측정이 필요한 일은 "실험" 양식으로 이슈를 엽니다. 가설이 맞을 때와 틀릴 때 각각 무엇을 할지 먼저 적습니다.
- 결과는 `docs/experiments/`에 [실험 보고서 양식](docs/templates/experiment-report.md)으로 남깁니다. 가설이 틀린 결과도 남깁니다.
- 반복 횟수가 적으면 비율을 퍼센트가 아니라 건수(`7/12`)로 씁니다. 결과의 적용 범위(저장소·모델·과제)를 한정해서 씁니다.

## 이 저장소에서 쓰는 측정 용어

| 용어 | 이 저장소에서의 뜻 | 헷갈리기 쉬운 말 |
|---|---|---|
| 종단 시간 | 요청(또는 계획 승인)부터 마지막 게이트 결과까지 벽시계 시간 | 처리량(단위 시간당 끝낸 작업 수)과 다릅니다 |
| 기동 시간 | 샌드박스 생성부터 서비스 준비 판정까지 | 종단 시간에 포함되지만 따로 적습니다 |
| 입력 토큰 / 출력 토큰 / 캐시 읽기 토큰 | 제공자 응답의 usage 값 | 합쳐서 "토큰"이라고만 쓰지 않습니다 |
| 추정 비용 | 모델 레지스트리 단가 × 토큰. 청구서 금액이 아닙니다 | |
| 컨텍스트 크기 | 모델 호출 한 번의 입력 토큰 | 대화 전체 토큰 합과 다릅니다 |
| 동시 실행 수(concurrency) | `runTaskGraph`가 한 번에 돌리는 작업 수의 상한 | 실제로 동시에 떠 있는 샌드박스 수와 다를 수 있습니다 |
| 병렬(parallelism) | 서로 다른 샌드박스에서 실제로 같은 시각에 돈 것 | |
| 게이트 통과 | `run·contract_check·browser_check·test·review` 중 필수 단계가 모두 통과 기록을 남김 | 모델의 완료 메시지가 아닙니다 |
| 통합 성공 | 작업 분해 통합 세션이 게이트를 통과함 | |

## 변경 원칙

- 명세는 `spec`, 실행 경계는 `sandbox`, 모델 루프와 게이트는 `agent`, UI와 세션 API는 `studio`에 둡니다.
- 외부 입력은 스키마에서 검증하고 경로·URL·환경 변수 이름을 좁게 허용합니다.
- 모델의 완료 메시지를 신뢰하지 않고 플랫폼이 관찰할 수 있는 결과로 판정합니다.
- 실패, 취소, 프로세스 종료 뒤에도 사용자 코드와 DB가 일관된 상태로 돌아오는지 확인합니다.
- 시크릿 값, 인증 토큰, 개인정보를 로그·오류·체크포인트·fixture에 넣지 않습니다.
- 공개 동작이나 설정을 바꾸면 README 또는 `docs/`를 함께 고칩니다.

## 테스트 선택

| 변경 영역 | 최소 확인 |
|---|---|
| `packages/spec` | 관련 단위 테스트, `pnpm typecheck` |
| `packages/sandbox` | 단위 테스트, 가능하면 실제 Docker 재현 |
| `packages/agent` | 단위 테스트, 게이트 변경은 `pnpm e2e:agent` |
| `apps/studio` | 타입 검사, lint, production build, 관련 브라우저 흐름 |
| 템플릿 | 템플릿 자체 빌드와 `examples/orders` 기동 |
| 문서만 | 링크, 명령, 파일 경로가 현재 저장소와 맞는지 확인 |

## 설계 기록

보안 경계, 완료·복구 의미, 장기 제약을 만드는 선택을 바꾸면 [설계 결정 기록](docs/decisions.md)에 새 ADR을 추가합니다. 기존 ADR의 당시 판단을 지우지 말고 새 ADR에서 대체 관계를 설명합니다.

## Pull Request

PR은 [PR 템플릿](.github/pull_request_template.md)을 따릅니다. 무엇을·왜·선택지와 결정·변경·검증·돌리지 않은 검증과 이유·예상과 실제·위험과 되돌리기·확인 절을 채웁니다. 확인 체크리스트는 템플릿의 "확인" 절에 있습니다.

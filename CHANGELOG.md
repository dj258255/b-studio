# 변경 기록

이 문서는 사용자가 알아볼 변경을 날짜 순으로 적습니다. [Keep a Changelog](https://keepachangelog.com/) 형식을 따르되, 아직 릴리스 버전이 없어 날짜(KST, UTC+9)로 묶습니다. 2026-09-24 이전 항목은 PR·커밋 기록으로 나중에 재구성한 것입니다. `docs:`, `chore:`처럼 사용자 동작에 영향이 없는 커밋은 `### 문서`로 모으거나 생략했습니다. PR로 병합된 변경은 끝에 `(#번호)`를, PR 없이 main에 직접 들어간 커밋은 끝에 짧은 해시를 붙였습니다. 병합 커밋 제목에 `(#번호)`가 없던 과거 PR도 병합 커밋 해시 대응으로 PR 번호를 되찾아 적었습니다.

## 아직 릴리스하지 않음

### 추가

- 착수 명세·버그·실험 이슈 양식 3종과 PR 템플릿을 추가했습니다 (#46).
- 프로젝트 로드맵(`ROADMAP.md`)과 변경 기록(`CHANGELOG.md`)을 추가했습니다 (#46).
- ADR 양식(`docs/templates/adr.md`)과 실험 보고서 양식(`docs/templates/experiment-report.md`)을 추가했습니다 (#46).

### 변경

- `CONTRIBUTING.md`에 작업 흐름·예상 갱신·실험·측정 용어 절을 더하고, PR 체크리스트를 PR 템플릿으로 옮겼습니다 (#46).

### 문서

- `README.md`, `docs/README.md`, Wiki 개발 페이지의 문서 안내를 로드맵·변경 기록·문서 양식에 연결했습니다 (#46).

## 2026-09-24

### 변경

- 스튜디오 화면의 흔한 화면 문법을 모서리·그림자 토큰으로 정리했습니다 (#40).

## 2026-09-18

### 문서

- README에 일정과 작업 방식 절을 추가했습니다 (#41).

## 2026-09-16

### 추가

- 검증 게이트가 워크플로 단계를 강제하고, 스튜디오가 워크플로 검사 결과를 보여 주며 릴리스 단계가 빠진 배포를 막습니다 (cdac5c6, 76e7b99).
- 작업 분해: 계획을 사람이 승인해야 레인을 실행하고 (7122c82), 독립 레인을 병렬로 돌린 뒤 통합 결과를 같은 게이트로 재검증합니다 (67ca206). 통합만 남은 계획은 스튜디오 재시작 뒤 이어서 재시도할 수 있습니다 (1eeea9a).
- 레인이 지운 파일을 정책 검사를 거치는 도구로 처리하고 통합에 반영합니다 (5a7e439).
- 헤드리스 브라우저 화면 확인과 작업 분해의 핵심을 추가하고 (bda5240), 화면 확인에 클릭·입력 단계를 더했습니다 (76f55f1).
- Pi 내장 도구 호출을 같은 정책 코드로 막고 다음 행동을 안내하는 Pi 정책 브리지를 추가했습니다 (675cac1).
- `studio verify`로 편집기에서 바꾼 현재 변경도 에이전트와 같은 게이트에 넣어 확인합니다 (fe3fcf2).
- 예제 api의 워크플로 테스트 단계에 슬라이스 테스트를 추가했습니다 (2eed4ea).

### 수정

- 워크플로 트레일러를 스튜디오가 만든 체크포인트에서만 신뢰합니다 (230c186). 본문이 잘리거나 요약이 위조돼도 안전하게 남깁니다 (3eb9fc2).
- Pi 브리지에서 시크릿 파일 읽기를 차단합니다 (3ae1570).
- 세션을 없애거나 정리할 때 남은 샌드박스 이미지를 지웁니다 (1d516f9). 남은 샌드박스 자원을 정리하고 디스크 부족 실패를 설명합니다 (e147110).

### 문서

- 브라우저 확인, 작업 분해, 샌드박스 이미지 누수를 기록했습니다 (9c34012). 워크플로 강제, 릴리스 규칙, Pi 브리지 한계를 적었습니다 (da35847).

## 2026-09-15

### 추가

- 도구 호출 전에 실행 정책을 적용합니다 (631facd).

### 문서

- b-studio를 에이전트 개발 환경(ADE)으로 위치시켰습니다 (fdfabd1).

## 2026-09-14

### 문서

- 저장소 안내를 재구성하고 Wiki를 추가했습니다 (4959a54).

## 2026-09-13

### 추가

- 모델 라우팅과 Agent Fleet을 추가했습니다 (4eb01ff). 여러 모델을 같은 도구 계약으로 실행하고, 같은 요청을 독립 브랜치·샌드박스에서 병렬로 돌려 결과를 비교합니다.

## 2026-09-12

### 추가

- 운영 이미지 빌드와 무중단 배포를 추가했습니다 (#27). 체크포인트를 운영에 올리는 배포 탭을 추가했습니다 (#28).
- PR CI와 스튜디오 컨테이너 이미지를 추가했습니다 (#29).
- 파일을 바꾸지 않는 질문 모드를 추가했습니다 (#26).
- 코드 탭에서 목록 쪽 넘기기와 내용 찾기를 추가했습니다 (#31).
- 에이전트 답변의 수식 렌더링을 추가하고 각주 링크를 고쳤습니다 (#32).
- 사람·기간 단위 토큰 한도를 추가했습니다 (#33).
- 정책 프록시가 자유 텍스트 속 개인정보를 값 형태로 가립니다 (#35).

### 수정

- 로그아웃 무효화, 해시 토큰, 로그인 실패 제한, 미리보기 접근 확인으로 인증을 강화했습니다 (#30).
- Gradle 홈을 샌드박스마다 분리해 같은 프로젝트 동시 세션을 허용했습니다 (#34).
- 인코딩된 구분자가 든 경로는 규칙을 검사할 수 없어 막습니다 (#38).

### 변경

- egress 규칙에 경로와 메서드를 적용합니다 (#36).
- 데이터베이스 덤프를 파일로 흘려 256MB 상한을 없앴습니다 (#37).

## 2026-09-11

### 추가

- 세션 브랜치를 원격에 올리고 PR을 만드는 흐름을 추가했습니다 (#4).
- 로컬 로그인 계정으로 에이전트를 실행하는 backend를 추가했습니다 (#3).
- 체크포인트마다 같은 시점의 DB 상태를 남기는 DB 브랜치를 추가했습니다 (#6).
- 자원 한도와 사용량 표시를 추가했습니다 (#7).
- 네트워크 격리와 edge 출입구 컨테이너를 추가했습니다 (#8).
- 시크릿 주입과 출력 가림을 추가했습니다 (#9).
- 사내 API 정책 프록시를 추가했습니다 (#10).
- gVisor 컨테이너 런타임을 추가했습니다 (#11).
- Kubernetes 제공자를 추가했습니다 (#12).
- 세션 복구와 남은 샌드박스 정리를 추가했습니다 (#13).
- 호스트 이름으로 나누는 원격 미리보기 게이트웨이를 추가했습니다 (#17).
- 모노레포 하위 폴더 프로젝트의 원격 연동을 추가했습니다 (#16).
- 조작 계층을 유리로 띄운 화면 디자인과 다크 모드를 추가했습니다 (#15).
- 코드 탭에서 에이전트가 쓰는 코드를 실시간으로 보여 줍니다 (#18). 코드 탭과 diff에 문법 강조를 넣고 (#20), 파일 찾기와 변경 감시를 추가했습니다 (#23).
- 처리 중인 요청 취소와 토큰 사용량 표시를 추가했습니다 (#19).
- 에이전트 답변을 마크다운으로 그립니다 (#21).
- 세션 토큰 한도를 추가했습니다 (#22).
- 스튜디오 인증과 세션 권한을 추가했습니다 (#24).
- 내 폴더에서 바로 작업하는 세션을 추가했습니다 (#25).

### 변경

- 기동 스냅샷을 적용해 측정으로 찾은 병목을 줄였습니다 (#5).

### 수정

- 원격 변경 가져오기와 반영 확인을 고쳤습니다 (#14).

## 2026-09-10

### 추가

- pnpm 워크스페이스와 TypeScript 설정을 갖췄습니다 (c733cc8).
- `studio.yaml` 스키마와 compose 교차 검증 로더를 추가했습니다 (4a0d0b4).
- 샌드박스 인터페이스, 준비 판정, 로컬 Docker 제공자를 추가했습니다 (0aefdf9).
- Next.js 16 · Spring Boot 4.1 · FastAPI 개발용 템플릿을 추가했습니다 (3262618).
- `studio up` 명령과 orders 예제 프로젝트를 추가했습니다 (4499334).
- 파일 반영 확인, 최근 로그 조회, exec 중단 신호를 추가했습니다 (2552bbe).
- `studio agent` 명령과 샌드박스 세션 공통화를 추가했습니다 (07af3cf).
- 웹 스튜디오(대화, 미리보기, API 탐색기, 로그)를 추가했습니다 (#1).
- 세션 체크포인트와 실패한 변경 되돌리기를 추가했습니다 (#2).

### 문서

- README, 설계 결정 기록, 트러블슈팅을 추가했습니다 (6d6dd8d).
- 에이전트 루프 설계 결정, 검증 결과, 게이트 버그 트러블슈팅을 적었습니다 (28b1e9f).

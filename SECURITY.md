# 보안 정책

## 취약점 제보

공개 Issue에 취약점의 재현 코드, 토큰, 개인정보, 내부 주소를 올리지 마세요. 현재 별도 보안 연락처가 공개되어 있지 않으므로 저장소 소유자에게 비공개 연락 수단을 요청한 뒤 영향을 받는 커밋, 재현 절차, 기대한 경계, 실제 결과, 가능한 완화 방법을 전달해 주세요.

## 보안 경계

b-studio는 프로젝트별 샌드박스, 자원 한도, edge 프록시, 허용 목록 기반 egress, 제한된 에이전트 도구, 시크릿 마스킹, external API 정책, 세션 소유권과 Origin 검증을 제공합니다.

이 기능이 모든 운영 위험을 제거하지는 않습니다.

- Docker 소켓은 사실상 호스트 수준 권한입니다. 서버와 접근 사용자를 신뢰 경계 안에 두세요.
- `claude-code` 모드는 개인 PC 전용입니다. 공유 서버에서는 API 모드와 웹 인증을 사용하세요.
- 인증 프록시 모드에서는 외부 요청이 앱에 직접 닿지 않도록 네트워크에서 강제하세요.
- 모델 키와 프로젝트 시크릿은 저장소가 아닌 시크릿 관리자에서 주입하세요.
- `network.egress`와 external API 정책은 최소 권한으로 작성하세요.
- 세션 기록의 암호화, 보존 기간, 삭제 정책을 별도로 정하세요.
- Kubernetes에서는 RuntimeClass, NetworkPolicy, RBAC, 볼륨, 이미지 출처를 검증하세요.
- 배포 롤백은 DB 마이그레이션을 되돌리지 않습니다.

현재 별도 릴리스 버전과 장기 지원 정책이 없습니다. 보안 수정은 기본 브랜치의 최신 상태를 기준으로 관리합니다.

신뢰 경계는 [아키텍처](docs/architecture.md), 운영 설정은 [운영과 배포](docs/operations.md), 관련 선택은 [ADR-024 이후](docs/decisions.md#adr-024-네트워크-격리-샌드박스의-출입구를-하나로-만든다)를 참고하세요.
